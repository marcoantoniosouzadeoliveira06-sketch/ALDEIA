"// app.js — ALDEIA Design | Todos os efeitos react-bits portados para Vanilla JS\n// ─────────────────────────────────────────────────────────────────────────────\n// 1. Preloader Cinematográfico\n// 2. Cursor Personalizado (lerp)\n// 3. BlurText          → hero-tag + hero-title\n// 4. TrueFocus         → manifesto h2\n// 5. FluidGlass        → nav bar (mouse position)\n// 6. SpotlightCard     → portfolio items (spotlight segue mouse)\n// 7. DomeGallery       → portfolio grid (perspectiva côncava)\n// 8. BorderGlow        → service items (borda giratória)\n// 9. ProfileCard       → contato (tilt 3D + glow)\n// 10. PillNav          → nav (pílula deslizante)\n// 11. Scroll Reveal    → animações gerais de entrada\n// 12. Scroll Spy       → link ativo na nav\n// 13. Stats Counter    → contagem animada\n// ─────────────────────────────────────────────────────────────────────────────\n\ndocument.addEventListener('DOMContentLoaded', () => {\n\n    // ════════════════════════════════════════════════\n    // 1. PRELOADER CINEMATOGRÁFICO\n    // ════════════════════════════════════════════════\n    const OPENING_DELAY = 2500;\n\n    setTimeout(() => {\n        document.body.classList.add('loaded');\n        setTimeout(() => {\n            const preloader = document.getElementById('preloader');\n            if (preloader) preloader.style.display = 'none';\n        }, 2000);\n    }, OPENING_DELAY);\n\n\n    // ═══════════════════════════════
<truncated 23566 bytes>